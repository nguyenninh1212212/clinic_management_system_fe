import React from 'react';
import { FormControl, InputLabel, MenuItem, Select, TextField } from '@mui/material';
import { AppointmentStatus } from '@/types';

interface ScheduleFiltersProps { date: string; search: string; specialtyId: number | ''; status: AppointmentStatus | ''; specialties: { id: number; name: string }[]; onDateChange: (value: string) => void; onSearchChange: (value: string) => void; onSpecialtyChange: (value: number | '') => void; onStatusChange: (value: AppointmentStatus | '') => void; }

export const ScheduleFilters: React.FC<ScheduleFiltersProps> = ({ date, search, specialtyId, status, specialties, onDateChange, onSearchChange, onSpecialtyChange, onStatusChange }) => <div className="grid grid-cols-1 gap-3 rounded-xl border border-slate-200 bg-white p-4 sm:grid-cols-2 lg:grid-cols-4"><TextField size="small" type="date" label="Ngày xem lịch" value={date} onChange={(event) => onDateChange(event.target.value)} slotProps={{ inputLabel: { shrink: true } }} /><TextField size="small" label="Tìm bác sĩ" value={search} onChange={(event) => onSearchChange(event.target.value)} /><FormControl size="small"><InputLabel id="schedule-specialty">Chuyên khoa</InputLabel><Select labelId="schedule-specialty" label="Chuyên khoa" value={specialtyId} onChange={(event) => onSpecialtyChange(event.target.value as number | '')}><MenuItem value="">Tất cả chuyên khoa</MenuItem>{specialties.map((specialty) => <MenuItem key={specialty.id} value={specialty.id}>{specialty.name}</MenuItem>)}</Select></FormControl><FormControl size="small"><InputLabel id="schedule-status">Trạng thái lịch</InputLabel><Select labelId="schedule-status" label="Trạng thái lịch" value={status} onChange={(event) => onStatusChange(event.target.value as AppointmentStatus | '')}><MenuItem value="">Tất cả trạng thái</MenuItem>{Object.values(AppointmentStatus).map((value) => <MenuItem key={value} value={value}>{value}</MenuItem>)}</Select></FormControl></div>;